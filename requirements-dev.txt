-r requirements-build.txt

# Build dependencies
# NOTE: These should match the packages in pyproject.toml's project.dependencies. We use a separate
requirements file since if we try to use pip to install the dependencies from the pyproject.toml
file, it will also unnecessarily build and install clp_ffi_py.
python-dateutil >= 2.7.0
typing-extensions >= 4.1.1
zstandard>=0.18.0

# Test dependencies
smart_open>=6.3.0

# Lint requirements
black>=23.11.0
clang-format>=17.0.5
docformatter>=1.7.5
mypy>=1.7.1
mypy-extensions>=1.0.0
ruff>=0.1.6
types-python-dateutil>=2.8
yamllint>=1.35.1
